# Mapping Examples

These files are optional examples for `sync_i18n_locale.py`.

- `generic.mapping.json`: framework-agnostic starter mapping.

Use any file here as a starting template and copy/adjust for your own project.
